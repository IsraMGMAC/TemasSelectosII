Primer comando con Maven: mvn clean install

mvn spring-boot:run
Si no corre este comando,ver que no este corriendo otro servicio en el puerto 8080

Instalar postman

Dependencia JPA ayuda al manejo de sentencias SQL's

