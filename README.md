# Proyecto Online (Spring Boot)

Proyecto de ejemplo en Java con Maven (Spring Boot).

Instrucciones rápidas para subir a Git:

1. Inicializar repo local: `git init`
2. Añadir remoto (GitHub/GitLab): `git remote add origin <URL>`
3. Subir rama principal: `git push -u origin main`
